"use strict"

//created and Array (list) of family member names
let myFamilyArray = ["Eric", "Chanda", "Cash", "Uncle Fester"];

//get the number of items on the list (in this specifc case it should be 4)
let numberOfFamilyMembers = myFamilyArray.length;

//loop over my family array and print the names
for(let i=0; i < numberOfFamilyMembers; i++){

    //pring out each family members name
    console.log(myFamilyArray[i]);

}